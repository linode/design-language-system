import * as TokensNested from '../../dist/tokens-nested.es6.js';
import type {
  GlobalTypes
} from '../../dist/tokens-nested';

export const {
  Color,
  Spacing,
  Font,
} = TokensNested.default.Global as GlobalTypes;
